// /* eslint-disable prettier/prettier */
// import { createReducer, on } from '@ngrx/store';
// import { setAccountList } from '../actions';
// // import { initialAccountList } from '../states/app.state';

// // export const listOfAccountReducer = createReducer(
//   // // initialAccountList,
//   // on(setAccountList, (state, { accountList }) => ({
//   //   ...state,
//   //   ...accountList,
//   // })),
// );
