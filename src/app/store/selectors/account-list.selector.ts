// import { createFeatureSelector, createSelector } from '@ngrx/store';
// import { AppState } from '../states/app.state';
// import { ListOfAccountState } from '../states/account-list.state';

// export const listofAccountState = createFeatureSelector<ListOfAccountState>('listOfAccounts');

// // export const selectListOfAccounts = createSelector(
// //   listofAccountState,
// //   (state: AppState) => state.accountList,
// // );
